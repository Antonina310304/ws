mixin _button(text, mixin)
    button(type="button" class=`${mixin}`).button=text

mixin _buttonSubmit(text, mixin)
    button(type="submit" disabled class=`${mixin}`).button=text

mixin _buttonDeep(mixin)
    button(type="button" class=mixin).button
        block

mixin buttonBlue(text)
    +_buttonSubmit(text, 'button--blue')

mixin buttonBlueScrollTo(text, scrollTo)
        button(type="submit" data-scroll=`${scrollTo}`).button.button--blue=text

mixin buttonModal(text)
    button(type="submit" class=`button--blue js-modal-close`).button=text

mixin buttonGradient(text)
    button(type="button" class='button--gradient').button&attributes(attributes)
        span.text-gradient=text
        span.button__hover.text-white=text

mixin buttonGradientBig(text)
    button(type="button" class='button--gradient big').button&attributes(attributes)
        span.text-gradient=text
        span.button__hover.text-white=text

mixin button(text)
    +_button(text, '')

mixin buttonAut(text)
    button(data-toggle="modal" data-target="authorization").button=text

mixin buttonWhite(text)
    +_button(text, 'button--white')

mixin buttonFaq(text)
    button(type="submit" data-scroll=`callback-form`).button.button--white.button--big=btnText.faq

mixin buttonCallback(text)
    button(type="button" data-toggle="modal" data-target="callback").button.button--white=text

mixin buttonLogin()
    button(type="button" class="button--icon" data-toggle="modal" data-target="authorization").button
        span Личный кабинет
        i._icon
            +loginSvg


mixin btnText(text, attr, val)
    button&attributes({attr: val})=text

mixin buttonAttr(param)
    button(class=`${param.mixin}`)&attributes(param.attr).button=param.text

